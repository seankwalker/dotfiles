interface:
  display_name: "Sync Work"
  short_description: "Sync worker progress into today’s daily note"
  default_prompt: "Use $sync-work to reconcile worker-session progress into today’s daily note."
policy:
  allow_implicit_invocation: false
