# Kindo MCP review gates

Classify the change before evaluating it: unified Python integration (`integrations/`, `/Users/sean/Developer/mcp/integrations/AGENTS.md`); remote-proxy exception (`remote-proxies/`); legacy maintenance (`legacy-integrations/`); or standalone vendor server (`vendor/`). Scope one expected server and its needed standard registration, catalog, repository delivery/review artifacts, and root lockfile changes only. Check catalog edits using the `integration-catalog` skill and its validators.

For Python integrations, require the package/client/tools/errors/server/tests conventions documented in `/Users/sean/Developer/mcp/integrations/AGENTS.md`, `/Users/sean/Developer/mcp/.claude/skills/code-standards-python/SKILL.md`, and `/Users/sean/Developer/mcp/.claude/skills/local-review-patterns/SKILL.md`. Authentication must use `kindo-mcp-utils`/Nango; reject bespoke token exchange, storage, or credential persistence. Verify dependencies and the root lockfile move together.

A unified Python integration normally has its `pyproject.toml` and `justfile`, a `src/` module with `py.typed`, client/tools/errors/server modules, focused tests without `tests/__init__.py`, and root registration/mount changes. Require request-scoped auth from `kindo_mcp_utils`, a canonical error factory, tool/error/server/security tests, and all four MCP side-effect annotations. Escalate missing or custom shapes rather than accepting a near match.

A remote proxy normally uses `FastMCPProxy(client_factory=...)`, creates a fresh client per request, and sets `forward_incoming_headers=True`; it normally has no custom client/tools/errors/models layers. `Salesforce create_proxy(url)` is deprecated and is not a reference pattern. Legacy integrations are maintenance-only; vendor servers must preserve contextvar/stateless credential isolation. Escalate a new custom legacy/vendor shape.

Review tool design against `/Users/sean/Developer/mcp/docs/TOOL_DESIGN_PRINCIPLES.md` and `/Users/sean/Developer/mcp/docs/TOOL_DESIGN_PROCESS.md`: P1–P12 and A1–A12 apply. Expected budgets: response ≤25K characters (target ≤10K), list default 10–25 with maximum ≤100, write acknowledgement ≤~500 characters, ≤~20 tools, ≤~15 list fields, and usually ≤2–3 required parameters. Cite the conversion recipe for a violation.

Require applicable package/root CI, catalog validation where catalog changes, Docker build evidence (not a runtime smoke), and the advisory security workflow. A new integration also needs live-provider E2E and response-size evidence recorded as repository delivery evidence under `/Users/sean/Developer/mcp/docs/AGENCY_DOCUMENTATION_GUIDE.md`. A merged green PR is not independent runtime proof. Compare applicable patterns in `/Users/sean/Developer/mcp/.claude/skills/create-integration/reference-patterns.md`, current integrations, and the Monday/Dynatrace proxy patterns in `/Users/sean/Developer/mcp/remote-proxies/AGENTS.md` as patterns, not as proof.

Use `/Users/sean/Developer/mcp/.github/workflows/`, `/Users/sean/Developer/mcp/justfile`, and `/Users/sean/Developer/mcp/justfile.shared` to map expected CI. Treat missing, failed, pending, or ambiguous required evidence as ESCALATE unless there is a concrete defect, which is FAIL.

Map verification to the category: unified Python needs package `just check`, whole Python CI, catalog validators when applicable, Docker image/Helm build, live-provider E2E, and worst-case response-size evidence. Use the corresponding TypeScript or vendor checks for those categories; Docker build evidence never substitutes for a runtime smoke.
